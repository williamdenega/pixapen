import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Pixelify Sans, Luckiest Guy, Arial, sans-serif',
  },
  // Optionally, you can customize other aspects of the theme here
});

export default theme;
